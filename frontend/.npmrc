# Use --omit=dev instead of deprecated --production flag
omit=dev

# Suppress npm warnings about deprecated production flag
loglevel=error

# Suppress the specific production flag deprecation warning
fund=false
audit=false

