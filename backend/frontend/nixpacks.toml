[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
# Install all dependencies (including dev) for build
cmds = [
  "npm ci"
]

[phases.build]
cmds = [
  "npm run build"
]

# For production, we don't need to reinstall, but Railway handles this

